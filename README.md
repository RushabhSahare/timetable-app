# timetable-app

## Install Python & pip
sudo apt update
sudo apt install python3 python3-pip -y

## Clone repository
git clone https://github.com/RushabhSahare/timetable-app.git
cd timetable-app

## Install dependencies
pip install -r requirements.txt

## Run the app
python3 app.py
